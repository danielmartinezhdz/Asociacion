<%= form_for(users_profile) do |f| %>
  <% if users_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(users_profile.errors.count, "error") %> prohibited this users_profile from being saved:</h2>

      <ul>
      <% users_profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :profile_id %>
    <%= f.text_field :profile_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
